# Job Application Tracker

Full-stack job application tracker built with Next.js (App Router), MongoDB and JWT auth.

> Work in progress — see the completed sections below as features land.

## Run it

```bash
cp .env.example .env
docker compose up --build
```

The UI is served on http://localhost:3000.
