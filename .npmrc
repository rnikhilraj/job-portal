# Refuse to install on a runtime below the `engines` floor. Without this the
# mismatch only surfaces later, as `ReferenceError: File is not defined` from
# suites that rely on the global `File` Node exposes from 20 onward.
engine-strict=true
